/** Public marketing routes — statically generated and edge-cacheable. */
export const dynamic = 'force-static'
export const revalidate = 3600

export default function MarketingLayout({ children }) {
  return children
}
