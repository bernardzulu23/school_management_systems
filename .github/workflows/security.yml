# ZSMS Security Audit — runs on PRs, pushes to main/develop, and weekly.
#
# 1. Fails the build if HIGH/CRITICAL vulnerabilities exist in production deps.
# 2. Tenant-scoping static check + API handler smoke.
# 3. Runs the Vitest suite (includes __tests__/security/*).
# 4. Scans the diff for committed secrets.
#
# Fast PR gates (audit + tenant + Zod only) also live in pr-dependency-gates.yml.
name: Security Audit

on:
  push:
    branches: [main, develop]
  pull_request:
  schedule:
    - cron: '0 6 * * 1' # Mondays 08:00 Africa/Lusaka (UTC+2)

jobs:
  security-audit:
    name: Dependency audit + tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts
        env:
          HUSKY: '0'

      - name: Security audit (production deps, fail on high)
        run: npm run audit:security

      - name: Tenant scoping static check
        run: npm run check:tenant

      - name: API handler migration smoke (examples present)
        run: npm run check:api-handlers -- --fail-under 5

      - name: Run tests
        run: npm test
        env:
          JWT_SECRET: test-jwt-secret-minimum-32-characters-long
          JWT_REFRESH_SECRET: test-refresh-secret-minimum-32-characters
          NODE_ENV: test

  secret-scan:
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0 # full history for diff-based scanning

      - name: Scan for committed secrets
        uses: trufflesecurity/trufflehog@d411fff7b8879a62509f3fa98c07f247ac089a51 # v3.95.5
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified
