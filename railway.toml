[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node node_modules/prisma/build/index.js migrate deploy && node server.js || echo 'SERVER CRASHED WITH EXIT CODE '$?"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
