# Deprecated: Railway + Cloudflare

Use **[VERCEL_DEPLOY.md](./VERCEL_DEPLOY.md)** instead:

- **Neon** — PostgreSQL (`DATABASE_URL` pooled, `DIRECT_URL` for migrations)
- **Vercel** — Next.js app (Git integration or CLI)
