/** Inline script for layout — runs before React hydrates (avoids next-themes <script> in client tree). */
export const themeInitScript = `(function(){try{var k='theme',t=localStorage.getItem(k)||'system',r=t==='system'?(window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'):t;var d=document.documentElement;d.classList.remove('light','dark');d.classList.add(r);d.style.colorScheme=r;}catch(e){}})();`
